---
paths:
  - "backend/**/src/test/**/{integration, product, user}/**/*.java"
---

# 프레임워크 및 라이브러리 사용 규칙

- JUnit 5 (`org.junit.jupiter.api.*`) 및 AssertJ (`org.assertj.core.api.Assertions.*`) 기반 Fluent Assertion 작성
- Mocking은 Mockito (`org.mockito.*`) 활용
