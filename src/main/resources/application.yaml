spring:
  application:
    name: ecommerce
  datasource:
    url: jdbc:h2:mem:ecommerce-db
    username: sa
    password:
  jpa:
    hibernate:
#      ddl-auto: update
    show-sql: true
  h2:
    console:
      enabled: true
      path: /h2-console
